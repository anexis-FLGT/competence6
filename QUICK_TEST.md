# Быстрое тестирование через Swagger UI

## 🚀 Быстрый старт

1. **Откройте браузер**: http://localhost:8000/docs
2. Вы увидите интерактивную документацию API

## 📝 Пошаговое тестирование

### 1️⃣ Создайте категорию

1. Найдите **`POST /categories`** → Нажмите **"Try it out"**
2. Вставьте в Request body:
```json
{
  "name": "Художественная литература",
  "description": "Романы и рассказы"
}
```
3. Нажмите **"Execute"** → Запомните `id` (например, `1`)

### 2️⃣ Получите список категорий

1. Найдите **`GET /categories`** → **"Try it out"** → **"Execute"**
2. Увидите все созданные категории

### 3️⃣ Создайте книгу

1. Найдите **`POST /books`** → **"Try it out"**
2. Вставьте (используйте `category_id` из шага 1):
```json
{
  "title": "Война и мир",
  "author": "Лев Толстой",
  "description": "Роман-эпопея",
  "category_id": 1
}
```
3. Нажмите **"Execute"**

### 4️⃣ Протестируйте фильтрацию

1. Найдите **`GET /books`** → **"Try it out"**
2. Попробуйте разные фильтры:
   - `author`: `Толстой` → только книги Толстого
   - `title`: `Война` → книги с "Война" в названии
   - `category_id`: `1` → книги из категории 1
3. Нажмите **"Execute"** для каждого варианта

### 5️⃣ Обновите книгу

1. Найдите **`PUT /books/{book_id}`** → **"Try it out"**
2. Введите `book_id`: `1`
3. Измените Request body и нажмите **"Execute"**

### 6️⃣ Удалите книгу

1. Найдите **`DELETE /books/{book_id}`** → **"Try it out"**
2. Введите `book_id` → **"Execute"**

## ✅ Проверка Health Check

1. Найдите **`GET /health`** → **"Try it out"** → **"Execute"**
2. Должен вернуться: `{"status": "ok", "message": "API работает корректно"}`

## 📸 Не забудьте сделать скриншоты!

Сделайте скриншоты каждого шага и сохраните в `examples/`

